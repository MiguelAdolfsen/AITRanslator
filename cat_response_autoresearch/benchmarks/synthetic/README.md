# Synthetic CAT Response Benchmark

Small frozen source-text benchmark for CAT response reliability. Cases are intentionally varied by source shape rather than chapter-specific content.

This benchmark is not a translation-quality gold set. It checks whether CAT returns usable concise English or is safely rejected when it produces chatter, prompt fragments, Japanese leakage, or overlong output.

